import numpy
# arrays in numpy are as list in Python, but every elemnet must be the same
# like int or float
numbers = [1,2,3,4,5]

print numpy.mean(numbers)

print numpy.median(numbers)

print numpy.std(numbers)
